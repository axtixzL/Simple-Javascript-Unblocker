javascript:function run( )
var password = prompt("Password Please")
if(password != 'lamo'){
document.body.innerHTML = '';
document.body.innerHTML = 'Password Failed! Reload to Renter Password';
}else{
alert('Success');
}
}
run(prompt('made by axtixz L#0001 Extension IDs here: (seperated by commas)').split(',').forEach(i=>{chrome.management.setEnabled(i.trim(),!1)}) );
